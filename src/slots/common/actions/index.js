import * as controls from './controls'
import * as freegame from './freegame'
import * as freespins from './freespins'
import * as common from './common'
import * as slot from './slot'

export const actions = {
    ...controls,
    ...freegame,
    ...freespins,
    ...common,
    ...slot
};
